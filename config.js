export default {
    "print_mode": "tree", // can also be "json"
    "auto_create_models": false // auto create models from routes that are not yet known (thus making them known) (requires --allow-write)
}